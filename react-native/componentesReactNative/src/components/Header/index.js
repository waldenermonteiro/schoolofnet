import React from 'react';
import { View, Text } from 'react-native';
import { Containers as Container } from '../../../styles';

export const Header = () => (
  <View style={Container.Containers.headerContainer}>
    <Text>View 1</Text>
  </View>
);
